# SWE Evaluation Flow: swebench_tools_and_reasoning

This report summarizes the data flow and control flow when running a SWE evaluation using the `swebench_tools_and_reasoning` flow with the `claude-sonnet-4-20250514` model. The flow relies on structured tool calling and a reasoning model that supports native function calling.

## Overview

1. **Entry Script**: `scripts/run_evaluation.py` orchestrates the evaluation. It creates an `EvaluationManager` and uses a `SchedulerRunner` with `DockerRunner` to execute each instance inside a container.
2. **Flow Configuration**: The flow configuration resides in `.moatless/flows/swebench_tools_and_reasoning.json`. This JSON defines a `SearchTree` flow with a single `ActionAgent` that uses `claude-sonnet-4-20250514` via the `ToolCallCompletionModel`.
3. **Evaluation Manager**: `EvaluationManager.create_evaluation` builds the flow via `FlowManager.build_flow`, creates trajectories for each SWE-bench instance, and persists configuration and trajectory data to storage.
4. **Docker Runner**: For each instance, the `DockerRunner` spawns a container and executes `run_evaluation_instance` (defined in `moatless/runner/job_wrappers.py`). Inside the container, `run_swebench_instance` from `moatless/evaluation/run_instance.py` sets up the environment and runs the flow.
5. **Search Tree Execution**: `SearchTree.run` controls the search loop. It selects nodes (`_select`), expands them (`_expand`), simulates actions via the agent (`_simulate`), and backpropagates rewards (`_backpropagate`) until termination conditions are met.
6. **Runtime**: `setup_swebench_runtime` prepares a `SweBenchLocalEnvironment` that can apply patches and execute tests. The `Workspace` object mediates interactions with the repository and runtime environment.
7. **Evaluation**: After the flow finishes, `evaluate_instance` generates a patch from the selected node, applies it in the runtime, runs SWE-bench evaluation scripts, and records resolution status in the trajectory data.

## Key Files and Functions

- **Flow Configuration**: `.moatless/flows/swebench_tools_and_reasoning.json` defines the agent, model, and search tree parameters, including use of `claude-sonnet-4-20250514` and reasoning support.
- **Evaluation Script**: [`scripts/run_evaluation.py`](scripts/run_evaluation.py) sets up the evaluation and orchestrates Docker jobs.
- **Run Instance**: [`moatless/evaluation/run_instance.py`](moatless/evaluation/run_instance.py) executes a single SWE-bench instance within the container.
- **Search Tree Logic**: [`moatless/flow/search_tree.py`](moatless/flow/search_tree.py) implements the selection, expansion, simulation, and evaluation steps.

## Data Flow

1. **Configuration Loading**: `run_evaluation.py` loads the flow config and model settings via `FlowManager`. The configuration is stored in `.moatless/flows` and written to the evaluation project directory.
2. **Trajectory Creation**: For each SWE-bench instance, `EvaluationManager` creates a root node describing the problem and persists a `trajectory.json` file.
3. **Container Execution**: `DockerRunner` mounts `.moatless` into the container and runs `run_swebench_instance`. The container fetches the repository and index store, then executes the flow.
4. **Flow Execution**:
   - The agent receives the initial problem statement.
   - It uses native tool calling to request actions (e.g., patch generation, running tests) through the runtime environment.
   - The search tree iteratively expands nodes, simulating actions and evaluating rewards until termination criteria are met.
5. **Result Evaluation**: `evaluate_instance` applies the generated patch in the runtime, executes the SWE-bench evaluation harness, and stores the outcome (resolved or not) with timing information.
6. **Result Aggregation**: `EvaluationManager.process_evaluation_results` collects instance results and updates the evaluation status.

## Control Flow

- The outer loop in `run_evaluation.py` waits for all container jobs to finish and periodically calls `process_evaluation_results` to update status.
- Within each container, `SearchTree.run` forms the main control flow: select → expand → simulate → evaluate → backpropagate. Termination occurs when `is_finished` detects max iterations, cost, or other thresholds.
- Events emitted during flow execution are persisted and published via the event bus (handled in `moatless/flow/run_flow.py`).

## Conclusion

The `swebench_tools_and_reasoning` flow leverages the `claude-sonnet-4-20250514` model's native function calling abilities within a search-tree-based agentic loop. Docker containers provide isolated environments for running each SWE-bench instance, while the evaluation manager orchestrates instance creation, execution, and result aggregation.

